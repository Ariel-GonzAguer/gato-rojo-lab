---
// layout
import BaseLayout from "../layouts/BaseLayout.astro";

// componentes
import ContactFormEmailJS from "../componentes/ContactFormEmailJS";

// estilos
import "../styles/Contact.module.css";
---

<style>
  .contacto {
    padding: 1rem;
    text-align: center;

    & p {
      font-size: 1.4rem;
      line-height: 1.5;
      margin: 1rem auto 0;
      width: 75%;
      max-width: 800px;
      backdrop-filter: blur(5px);
    }
  }

</style>

<BaseLayout pageTitle="Contacto">
  <section class="contacto">
    <h1>Contacto</h1>
    <p>
      Nos encantaría trabajar con usted o su equipo para crear ese proyecto web
      que siempre ha estado en su mente.
    </p>
    <p>Envíe un mensaje y le contactaremos lo antes posible 🚀</p>
    <ContactFormEmailJS client:only="react" />
  </section>
</BaseLayout>
